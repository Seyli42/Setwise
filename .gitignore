# Bibliothèque vendorée, récupérée via la commande du README
frontend/dashboard/vendor/

# Variables d'environnement locales
.env
.env.local
.env.*.local

node_modules/
.DS_Store
._*

# Sauvegardes de configuration — contiennent les secrets en clair.
# Le dépôt est public : sans cette ligne, `git add -A` publierait la clé
# Stripe Live, le mot de passe Neon et les jetons Meta/Resend/DeepSeek.
.env.backup-*
.env.*.backup
*.env.bak
DEPLOY_ENV.txt
PUSH.sh
TESTER_EN_LOCAL.md
